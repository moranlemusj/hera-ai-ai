# All API calls are proxied through Vite's dev server (see vite.config.ts).
# Leave this empty in dev — overriding it only matters if you build for prod
# and serve the frontend from a different origin than the backend.
VITE_API_BASE=
